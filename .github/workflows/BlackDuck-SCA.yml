- name: Preflight - Validate BLACKDUCK_URL format
        shell: bash
        run: |
          URL="${{ vars.BLACKDUCK_URL }}"
          if [ -z "$URL" ]; then
            echo "BLACKDUCK_URL is empty"
            exit 1
          fi
          case "$URL" in
            */sca) echo "BLACKDUCK_URL must be the BASE host (no trailing /sca). Current: $URL"; exit 1 ;;
          esac
          echo "BLACKDUCK_URL looks good: $URL"

      # ✅ TEMP guard to prevent the Black Duck action's internal artifact upload from failing
      - name: TEMP GUARD - create legacy SARIF dir to avoid artifact failure
        if: always()
        shell: bash
        run: |
          mkdir -p ".bridge/Blackduck SCA SARIF Generator"
          : > ".bridge/Blackduck SCA SARIF Generator/.keep"

      - name: DEBUG - reached Black Duck step
        run: echo "About to run Black Duck SCA step..."

      - name: Black Duck SCA Scan
        uses: blackduck-inc/black-duck-security-scan@v2.1.1
        with:
          blackducksca_url:   ${{ vars.BLACKDUCK_URL }}
          blackducksca_token: ${{ secrets.BLACKDUCK_API_TOKEN }}
          blackducksca_prcomment_enabled: true
          github_token: ${{ secrets.GITHUB_TOKEN }}
          blackducksca_reports_sarif_create: true
          blackducksca_upload_sarif_report: false
        env:
          DETECT_PROJECT_NAME: ${{ github.event.repository.name }}
          DETECT_PROJECT_VERSION_NAME: ${{ github.ref_name }}

      - name: DEBUG - list .bridge after scan
        if: always()
        shell: bash
        run: |
          echo "Files under .bridge (depth 3):"
          find .bridge -maxdepth 3 -type f -print || true

      - name: Upload SARIF to Security tab (non-PR only)
        if: ${{ github.event_name != 'pull_request' }}
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: .bridge/blackduck_sca_sarif_generator/report.sarif.json

      - name: Upload Black Duck SARIF artifact (non-PR only)
        if: ${{ github.event_name != 'pull_request' }}
        uses: actions/upload-artifact@v4
        with:
          name: blackduck-sarif
          path: .bridge/blackduck_sca_sarif_generator/report.sarif.json
          if-no-files-found: error
